Memcache for caching -- assignment 2
so if we had a logins ystem - the key for this cache would be the -- Username and the value is loggIn -- which is just a boolean, could also have the session id as the key and the lookup would be the user object. 

GET should be idempotent -- used for retriving cache dimages. 

Whats good to cache  : image , suggestions(Amazon, Netflix, Disney +), Trending stuff
We cannot cache post requests - since they are non idempotent, 
cacheings a big deal - what takes time to genetrate and how do we reduce traffic. 


DDOS Attack -- Cpu time, Network, memory
